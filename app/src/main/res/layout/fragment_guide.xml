<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context="dk.danteater.danteater.guide.FragmentGuide">

    <android.support.v4.view.ViewPager
        android:id="@+id/guideSlider"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp">

        </android.support.v4.view.ViewPager>

    <dk.danteater.danteater.circle_indicator.CirclePageIndicator
        android:id="@+id/guideIndicator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|clip_horizontal"
        android:padding="16dp" />

    <dk.danteater.danteater.customviews.WMTextView
        android:id="@+id/btnStartUp"
        android:visibility="gone"
        android:padding="8dp"
        android:layout_margin="4dp"
        android:textSize="@dimen/normalText"
        android:background="@drawable/button_selector"
        android:textColor="@android:color/white"
        android:layout_gravity="bottom|right"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Afslut"
        />

    <dk.danteater.danteater.customviews.WMTextView
        android:id="@+id/btnSkip"
        android:visibility="gone"
        android:padding="8dp"
        android:layout_margin="4dp"
        android:textSize="@dimen/normalText"
        android:background="@drawable/button_selector"
        android:textColor="@android:color/white"
        android:layout_gravity="bottom|left"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Spring over"
        />

</FrameLayout>
